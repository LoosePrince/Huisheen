# Docker Compose 环境变量配置文件
# 复制此文件为 .env 并根据需要修改

# 应用配置
PORT=3000
NODE_ENV=production
WEBSITE_DOMAIN=localhost:3000

# JWT密钥（生产环境请使用强密钥）
JWT_SECRET=your_strong_jwt_secret_here_please_change_this_in_production

# MongoDB配置
MONGO_PORT=27017
MONGO_ROOT_USERNAME=admin
MONGO_ROOT_PASSWORD=password123

# 可选：外部MongoDB连接（如果使用外部MongoDB，注释掉上面的配置）
# MONGODB_URI=mongodb://username:password@external-mongo-host:27017/huisheen 